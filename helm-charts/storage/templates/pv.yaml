apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-pv
spec:
  capacity:
    storage: {{ .Values.storage }}
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "{{ .Values.hostPath }}"
  persistentVolumeReclaimPolicy: Retain
